//
// --- SceneGraph.js ---
//

function SceneGraph() {

    let root = function Node(object) {
        this.children = [];

        this.addChild(node) = function (object) { children.push(object); }
        this.removeChild(node) = function (object) {
            
        }
    }();

    this.root() = function () { return root; }
};